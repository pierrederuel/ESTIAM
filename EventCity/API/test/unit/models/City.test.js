"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/City');
const Fixture = require('../../fixtures/CityFixture');

describe('models:City', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
