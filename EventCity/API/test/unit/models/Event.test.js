"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Event');
const Fixture = require('../../fixtures/EventFixture');

describe('models:Event', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
