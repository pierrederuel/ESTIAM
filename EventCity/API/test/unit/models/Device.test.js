"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Device');
const Fixture = require('../../fixtures/DeviceFixture');

describe('models:Device', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
