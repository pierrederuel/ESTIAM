"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/CityController');

describe('controllers:CityController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
