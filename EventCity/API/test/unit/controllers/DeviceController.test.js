"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/DeviceController');

describe('controllers:DeviceController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
