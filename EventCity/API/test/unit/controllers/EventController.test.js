"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/EventController');

describe('controllers:EventController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
